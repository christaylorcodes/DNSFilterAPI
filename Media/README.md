# Media

Branding assets referenced by [`README.md`](../README.md) and the module manifest `IconUri`.

| File | Used by | Status |
| ---- | ------- | ------ |
| `Logo.png` | README header | DNSFilterAPI banner logo (550×140) |
| `Icon.png` | manifest `IconUri` (PowerShell Gallery icon) | DNSFilterAPI square icon (240×240) |

> A demo animation has not been added yet. When you record one, drop it in as `Demo.gif`
> and add it back to the README header.

Donation QR images and addresses are shared across projects and live in the
[template's `DONATE.md`](https://github.com/christaylorcodes/GitHub-Template/blob/main/DONATE.md)
rather than being duplicated here.
